# site-go

The main purpose is create site (html) for test http api resquest for public endpoints.

This is usually for CDN (aws Cloudfront), S3 web static and CORS tests.

The main page: index.html
The error page: error.html (this page is use in CDN behavior)

## Assets

A CloudFormation iaac the create a S3 bucket for web static content.